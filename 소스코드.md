# 제출물 ① 소스코드 — 저장소 구조

소스코드는 이 저장소 전체. 핵심 구현은 `isaac_ext/roboe_block_stacking/`.
베이스(Isaac Sim Franka Cortex 예제) 대비 **변경점은 전부 `[ROBOE]` 주석**으로 표시됨.

| 경로 | 내용 |
|---|---|
| `isaac_ext/roboe_block_stacking/` | **핵심 구현** — Isaac Sim GUI 예제 (씬 + 행동 + 인식) |
| ├ `perception/` | 카메라 · 검출기 4종 전환 · 3D 추정 · Cortex Bridge |
| ├ `behavior/` | 쌓기 의사결정 로직 (스톡 복사본, 순서·적재 위치만 수정) |
| └ `scene_setup.py` | 큐브/조명/고스트 구성 (학습 데이터 생성과 공용) |
| `scripts/` | 설치 스크립트 (`setup_runtime.sh`, `setup_zeroshot.sh`) |
| `sdg/` | Replicator 합성 데이터셋 생성 |
| `training/` | YOLO 학습 · 검증 · TorchScript export (별도 venv) |
| `models/` | 학습된 모델 (TorchScript 포함 — clone 후 바로 실행 가능) |
| `standalone/` | E2E 러너 · 단계별 검증 스크립트 |
| `eval/` | E2E 성공률 매트릭스 · zero-shot 오프라인 비교 · 단위 테스트 |
| `media/` | 실행 영상 · 그림 · 원자료 CSV |
| `docs/` | 구현 설명 문서 (단계별 4편) |

설치·실행 방법: [README](README.md) "직접 실행하기" 절.
